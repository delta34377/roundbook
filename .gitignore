# arccos-data-full.json contains GPS traces of every shot. The repo is
# private, but keep the raw export out of git anyway; regenerate it locally
# with arccos_export.py when needed.
arccos-data-full.json
__pycache__/
*.pyc
.env
.env.*
!.env.example
node_modules/
dist/
.DS_Store
